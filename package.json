{
  "name": "my-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "bun --cwd apps/web dev",
    "dev:api": "bun --cwd apps/api dev",
    "dev": "bun run dev:web & bun run dev:api",
    "lint": "bun --cwd apps/web run lint && bun --cwd apps/api run lint",
    "lint:fast": "bun --cwd apps/web run lint:fast && bun --cwd apps/api run lint:fast",
    "prepare": "node -e \"process.exit(0)\""
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8"
  }
}